spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cardapio
    username: postgres
    password: masterkey
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
  security:
    jwt:
      token:
        secret-key: 53cr37
        expire-length: 3600000


